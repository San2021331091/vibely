# Force JDK 17 only for this project
org.gradle.java.home=D:/Oracle/jdk-17

# Gradle JVM arguments optimized for 8GB RAM and enable native access
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED

# Enable AndroidX
android.useAndroidX=true
android.enableJetifier=true

# Optional: parallel build & caching for faster compilation
org.gradle.parallel=true
org.gradle.caching=true