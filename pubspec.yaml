name: vibely
description: "A flutter based video sharing social network app made with flutter,supabase and Fast.io."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.0
  simple_circular_progress_bar: ^1.0.2
  get: ^4.7.3
  cupertino_icons: ^1.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^5.0.0

flutter:

 
  uses-material-design: true

  
